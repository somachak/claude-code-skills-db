# Custom Semgrep Rule Authoring

**Category:** security-reliability · **Priority:** supporting · **Audience:** back-end, full-stack

Writes, tests, and ships custom Semgrep rules that catch project-specific security bugs and anti-patterns in TypeScript, Node, Python, FastAPI, and Django codebases. Use when adding lint coverage for a vulnerability class, codifying a postmortem, or building taint rules for user-input-to-sink flows.

## When Claude should reach for this

Trigger phrases: `semgrep rule`, `custom static analysis`, `taint mode`, `codify a postmortem`, `lint for security bug`, `semgrep --test`

## What you get

Open [`SKILL.md`](SKILL.md) for the full instructions Claude loads.
