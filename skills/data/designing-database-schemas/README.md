# Database Schema Designer — plain-English guide

## What this skill is for

Designs relational schemas, keys, constraints, and normalization tradeoffs for operational systems. Use when creating new tables, domain models, or storage-backed product features.

## In simple terms

This skill helps with database schema designer work so you do not have to explain the same rules or workflow every time.

## When you would use it

- database schema
- table design
- foreign keys
- normalization
- relational model

## Example things you could ask for

- Use the designing-database-schemas skill to help with this task.
- Designs relational schemas, keys, constraints, and normalization tradeoffs for operational systems. Help me when creating new tables, domain models, or storage-backed product features.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## Jargon explained

- `rag` — Retrieval-augmented generation, where an AI system looks up relevant documents before answering.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
