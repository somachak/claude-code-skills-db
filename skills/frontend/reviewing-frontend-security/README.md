# Frontend Security Review — plain-English guide

## What this skill is for

Reviews browser-facing code for XSS, token exposure, unsafe rendering, insecure storage, and client-side trust mistakes. Use when handling user content, auth state, embeds, or rich text.

## In simple terms

This skill helps with frontend security review work so you do not have to explain the same rules or workflow every time.

## When you would use it

- xss
- unsafe html
- token storage
- content security
- frontend auth

## Example things you could ask for

- Use the reviewing-frontend-security skill to help with this task.
- Reviews browser-facing code for XSS, token exposure, unsafe rendering, insecure storage, and client-side trust mistakes. Help me when handling user content, auth state, embeds, or rich text.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## Jargon explained

- `rag` — Retrieval-augmented generation, where an AI system looks up relevant documents before answering.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
