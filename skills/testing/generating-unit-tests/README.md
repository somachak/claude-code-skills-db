# Unit Test Generator — plain-English guide

## What this skill is for

Generates focused unit tests around branches, edge cases, and regressions without overfitting to implementation details. Use when extending logic, fixing bugs, or improving confidence in isolated modules.

## In simple terms

This skill helps with unit test generator work so you do not have to explain the same rules or workflow every time.

## When you would use it

- unit tests
- edge case tests
- regression test
- branch coverage

## Example things you could ask for

- Use the generating-unit-tests skill to help with this task.
- Generates focused unit tests around branches, edge cases, and regressions without overfitting to implementation details. Help me when extending logic, fixing bugs, or improving confidence in isolated modules.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## Jargon explained

- `rag` — Retrieval-augmented generation, where an AI system looks up relevant documents before answering.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
