# Caching Strategy Review — plain-English guide

## What this skill is for

Reviews cache key design, invalidation strategy, TTL choices, layering, and stale-read risks. Use when introducing Redis, CDN, query, or application-level caching.

## In simple terms

This skill helps with caching strategy review work so you do not have to explain the same rules or workflow every time.

## When you would use it

- cache invalidation
- redis
- ttl
- stale data
- cache key

## Example things you could ask for

- Use the reviewing-caching-strategies skill to help with this task.
- Reviews cache key design, invalidation strategy, TTL choices, layering, and stale-read risks. Help me when introducing Redis, CDN, query, or application-level caching.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
