# Webhook Integration Hardening — plain-English guide

## What this skill is for

Builds and reviews webhook consumers for signature verification, retries, deduplication, ordering issues, and replay safety. Use when integrating payment, messaging, or external event providers.

## In simple terms

This skill helps with webhook integration hardening work so you do not have to explain the same rules or workflow every time.

## When you would use it

- webhook
- signature verification
- replay attack
- event dedupe
- provider integration

## Example things you could ask for

- Use the handling-webhooks skill to help with this task.
- Builds and reviews webhook consumers for signature verification, retries, deduplication, ordering issues, and replay safety. Help me when integrating payment, messaging, or external event providers.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## Jargon explained

- `webhook` — A message sent automatically from one system to another when an event happens.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
