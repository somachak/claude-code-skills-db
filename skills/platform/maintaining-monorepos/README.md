# Monorepo Maintenance — plain-English guide

## What this skill is for

Maintains monorepos through workspace boundaries, task graph design, ownership rules, and incremental validation. Use when scaling packages, apps, services, and shared libraries together.

## In simple terms

This skill helps with monorepo maintenance work so you do not have to explain the same rules or workflow every time.

## When you would use it

- monorepo
- workspace graph
- nx
- turbo
- package boundaries

## Example things you could ask for

- Use the maintaining-monorepos skill to help with this task.
- Maintains monorepos through workspace boundaries, task graph design, ownership rules, and incremental validation. Help me when scaling packages, apps, services, and shared libraries together.

## What the files mean

- `SKILL.md` is the main instruction file that Claude Code uses.
- `references/` contains background guidance or checklists.
- `examples/` contains example patterns or sample outputs.
- `templates/` contains starting templates you can adapt.
- `scripts/` contains helper scripts when the skill needs repeatable checks or automation.

## Jargon explained

- `monorepo` — A single repository that contains multiple apps, packages, or services together.

## If you are unsure

Open `SKILL.md` only after reading this guide. If the words in `SKILL.md` feel too technical, use the example prompts above and keep your request in plain English.
